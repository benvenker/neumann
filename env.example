# Neumann Configuration
# Copy this file to .env and fill in your values

# Base URL for serving rendered page images (used in pages.jsonl URIs)
# Default: http://127.0.0.1:8000
ASSET_BASE_URL=http://127.0.0.1:8000

# ChromaDB storage path for persistent vector database
# Default: ./chroma_data
CHROMA_PATH=./chroma_data

# OpenAI API key (required for summaries and embeddings)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-key-here

# Chunking configuration (line-based chunks)
# Default: 180 lines per chunk, 30 lines overlap
LINES_PER_CHUNK=180
OVERLAP=30

