# Example environment configuration for Neumann
# Copy to .env and adjust values as needed

# Base URL used to construct HTTP URIs for rendered assets (dev server)
ASSET_BASE_URL=http://127.0.0.1:8000

# ChromaDB persistent storage path (local directory)
CHROMA_PATH=./chroma_data

# OpenAI API key (required for summaries/embeddings)
#OPENAI_API_KEY=sk-...

# Chunking parameters
LINES_PER_CHUNK=180
OVERLAP=30
